# NEXT PHASES — всё обсуждённое, но не сделанное

> Журнал решений и отложенных шагов из сессий июль–август 2026. Парный файл: `PROJECT_MAP.md`.
> Формат: что решили / почему отложили / что конкретно делать. Для переноса в Claude Desktop.

---

## ФАЗА 0 — Фронтир перед грантом (немедленное, порядок важен)

### 0.1 PUSH → проверка глазами → отправка обзора Claire
- **Статус:** 21 коммит готов локально (dd52f942..478a1af9). Пуш безопасен: Render мёртв/отвязан,
  пуш пересобирает только Vercel. Уходит 26 файлов: 4 новых контракта + тесты + деплой-скрипты,
  Rust attestor/verifier, весь фронт-редизайн (Accountability Layer секция, offline-state,
  честностные правки), 3 дока-разбора.
- **Шаги:** `git push` → открыть mantle-ai-swarm.vercel.app, проверить: секцию Accountability,
  ENGINE OFFLINE заметку, чат отвечает, тач/мобилка → отправить Claire текст обзора.
- **Текст 60-сек обзора Claire — ГОТОВ** (финальная версия в чате 04.08; лид: accountability layer
  через тезис Mantle «agent commerce», honesty: контракты «built and tested, mainnet next»).
  Обзор = ТЕКСТ, не видео (она перешлёт eco-команде одним сообщением). Видео опционально после
  пуша: hero → 3 steps → Accountability → живой чат (сценарий могу расписать).
- Короткая благодарность Claire («соберу и пришлю за пару дней») — черновик в чате, можно слать сразу.

### 0.2 Деплой 4 контрактов на mainnet — ГЛАВНЫЙ разрыв с финалистами
- **Статус:** готово всё, кроме самого деплоя. Runbook: `LAYER1_VERIFIER_SPEC.md §5`.
- Требует: `DEPLOYER_PRIVATE_KEY`; `SETTLER_ADDRESS` = ОТДЕЛЬНЫЙ кошелёк (иначе settleOutcome
  ревертит — анти-само-оценка); газ. Команды: `forge script script/DeployAttestor.s.sol ... --broadcast --verify`
  и то же для DeployVerifier. После деплоя фраза на сайте меняется «built and tested» → «live on mainnet».
- **Wire в swarm-engine** (после деплоя, = редеплой бэкенда): после [JUDGE]-вердикта слать
  `recordVerdict` (mantle-chain::attestor готов), класть `verdict_tx/inputs_hash/chain_hash/score`
  в log_entries телеметрии → **Verdict Explorer на фронте зажжётся сам** (progressive enhancement
  уже вшит в Decision Journal). Решить: писать каждый вердикт или только EXECUTED/REJECTED (газ vs полнота).

### 0.3 Новый бэкенд-сервер (Render умер)
- Поднять swarm-engine на новом хосте → на Vercel выставить env `VITE_TELEMETRY_URL` — код менять
  НЕ надо (вшито). Помнить: uptime/cycle начнут с нуля (in-memory). Обновить fallback-URL в
  useTelemetry.ts, когда хост станет постоянным. Проверить CORS и /telemetry формат.

---

## ФАЗА 1 — Решения, принятые но не исполненные

### 1.1 Направление B: «Agent Accountability Layer» (выбрано вместо гонки за consumer-UX)
- Рой = референс-имплементация слоя, продукт = слой подотчётности для ЧУЖИХ агентов.
- Клиент: билдеры агентов на Mantle + те, кому нужно агентам доверять.
- Монетизация (обсуждено, не решено): бонды/стейкинг, плата за верификацию, x402-микроплатежи.
- Первый шаг после деплоя: наш рой публично сидит на слое → потом первый чужой агент.

### 1.2 Риск-гейты: дефолты распущены (нашли при починке тестов)
- `EntryConfig::default()`: imbalance_long_min=0.0 (гейт МЁРТВ — ratio не бывает <0),
  imbalance_short_max=999, btc_dead_zone=0, loss_streak=10, exposure 95%.
- Логика гейтов корректна, тесты теперь пинят её через strict_config(). Но публично говорим
  «8 entry risk gates» — а часть no-op. **Решение оператора:** вернуть боевые пороги
  (возможно распускали под демо, как consensus threshold 1.5 в config/thresholds.toml).

### 1.3 $OUROBOROS токен = слой поверх OuroborosBond (не мем)
- Slash-триггер уже в коде (AgentViolation). Осталось: токеномика (стейкинг, governance над
  риск-параметрами, «paid seat as human juror» из RoadmapPage), ERC-20 вместо нативного MNT.

---

## ФАЗА 2 — Стратегические гэпы (из глубокого разбора финалистов)

### 2.1 Класс активов: мы торгуем крипто-пары — стратегия Mantle это RWA/xStocks
- Mantle: RWA TVL $247.5M (+27% Q1), 155 токенизированных акций на Fluxion, Franklin Templeton
  ETF $1.98B on-chain. Stax (winner) торговал xStocks, Argus (winner) защищал RWA.
- **Обсуждено:** возможный пивот/расширение на tokenized equities (Fluxion/xStocks) — крупное
  продуктовое решение, не начато.

### 2.2 Официальный агент-стек Mantle: используем 1 кубик из 5
- Есть: ERC-8004 ✓. Нет: AI Agent Skills, Agent Scaffold, **x402 payments (QuestFlow facilitator)**,
  ERC-8183 (agent commerce standard, Virtuals). «Mantle ecosystem fit» = критерий Гран-при.
- **x402 — приоритетный кандидат:** «фонд сам платит за свои данные/инференс через x402».
  У Gemini уже лежат untracked x402-крейты (consensus/liquidator/memory/polymarket/risk/sniper) —
  разобрать их состояние ПЕРЕД тем как строить своё (не дублировать чужую работу).

### 2.3 Продуктовые гэпы vs Stax (эталон)
- **Бизнес-модель не заявлена вообще** (у Stax: flat 25 bps). Решить и написать одну строку.
- Пользователь ничего не может СДЕЛАТЬ: нет депозита/wallet-connect. Layer 4 (gasless + social
  login, Privy/Pimlico) — **требует аккаунтов/ключей, каркас без них = фейк-кнопки, не строить вслепую.**
- Домен (не *.vercel.app). Имя-мисматч: repo `mantle-turing-ai-swarm` vs vercel `mantle-ai-swarm`.
- **Продуктовый README:** демо-GIF, «▶ Live at…», видео, секция accountability layer, скриншоты
  (README сейчас хакатонный; также reconcile с новыми контрактами).
- Пруф-квитанции после сделок (Stax: cryptographic receipt) — у нас появится бесплатно после 0.2 wire.

---

## ФАЗА 3 — Мелочи из аудитов (найдено, не исправлено)

- `.pipeline-stage` ячейки выглядят кликабельными (cursor:pointer), но кликов нет — или дать
  действие (открыть стадию), или убрать pointer.
- TimelineWave: hover-only подсветка вех, на таче недоступна.
- ESLint не сконфигурирован (v9 нужен eslint.config.js) — решить, нужен ли.
- Verdict-capsule (bull/bear в bento): hover-only на десктопе, есть index.css:2778 — проверить тач.
- `registryAddress` в телеметрии — dead state: приходит с бэка, нигде не рендерится (карточка
  хардкодит адреса). Либо рендерить, либо убрать.
- Битые лог-даты: logTime() синтезирует время от nowDate — норм для визуала, но не реальные
  таймстемпы записей (l.timestamp есть в LogEntry — можно показывать честные).
- Брендинг: «© Triarchy Labs» vs личный gmail + личный X — завести Triarchy-аккаунт/почту (решение юзера).

---

## ФАЗА 4 — Отношения с Mantle (статус диалога)

- **Claire:** ответила тепло 03.08. Ждёт обзор (0.1). Проверяет заметки судей — если пришлёт,
  превратить в новый to-do (как сделали с вопросами судей на Demo Day).
- **Deployment Awards вопрос** (мы прошли по критериям, но не в списке 20): задан Джемме 10.07,
  ответа нет, Джемма ушла из проекта. **Решение из чата: с Claire повторно НЕ поднимать**
  (душнило); поднять мягкой сноской только если сама спросит «чем помочь».
- Судейские скоркарты не публичны (проверено) — единственный канал = Claire.
- Гранты сейчас: открытых программ под нашу стадию НЕТ (подтверждено Claire + ресёрчем).
  Цель обзора = попасть на радар eco-команды, чтобы при открытии окна быть знакомым именем.
- Следить за X @Mantle_Official (анонсы builder-инициатив).

---

## ФАЗА 5 — Отложенное с хакатона (не забыть совсем)

- Layer 6 (виральный хук, Cult-стиль): один memorable момент — зрелищная визуализация ОТКАЗА
  роя от сделки. Только после Layer 1-3 live.
- Roadmap-страницы (Telegram-агент, Marketplace «Know-Your-Agent», Alpha feed) — копия есть,
  реализация не начата.
- Bybit-milestone в TimelineWave — проверить актуальность после пивота.
- Видео-демо оборудование: Voicebox RU→EN пайплайн жив (F9 push-to-talk, ~/.local/bin/voicebox-translate.py)
  — пригодится для любых англ. созвонов с Mantle.
